# ==============================================================================
# ALEXANDRIE - ENVIRONMENT CONFIGURATION
# ==============================================================================
# Instructions:
# 1. Copy this file to a new file named ".env"
# 2. Modify the values below to suit your environment
# 3. DO NOT commit your final ".env" file to source control
# ==============================================================================


# ------------------------------------------------------------------------------
# 0. Common Settings

# Public URLs used by the client's browser to reach the services.
# If using a Reverse Proxy (Nginx/Traefik), use your public domain names here.
FRONTEND_URL=http://localhost:8200
API_URL=http://localhost:8201
CDN_URL=http://localhost:9005

# Flags to enable or disable certain app features
CONFIG_DISABLE_LANDING=false # Disable the landing page if set to true
CONFIG_DISABLE_SIGNUP=false # Disable the signup page if set to true

# ------------------------------------------------------------------------------
# 1. DATABASE CONFIGURATION (MySQL)

MYSQL_EXTERNAL_PORT=3307 # The port exposed on your host machine for database access
MYSQL_ROOT_PASSWORD=rootpassword # Root password for the MySQL instance
MYSQL_DATABASE=alexandrie # The name of the application database

MYSQL_USER=alexandrie
MYSQL_PASSWORD=password

# ------------------------------------------------------------------------------
# 2. OBJECT STORAGE (RustFS / S3 Compatible)
RUSTFS_EXTERNAL_PORT=9005 # The port exposed on your host machine for S3 API access
MINIO_SECURE=false # Set to 'true' if your S3 endpoint uses HTTPS (true/false)

# Access credentials for the object storage
RUSTFS_ACCESS_KEY=alexandrie-key
RUSTFS_SECRET_KEY=alexandrie-secret
MINIO_BUCKET=alexandrie # The default S3 bucket name where files will be stored (alexandrie and alexandrie-backups are created by default)

RUSTFS_CONSOLE_ENABLE=false # Enable or disable the RustFS web management console (true/false)
RUSTFS_LOG_LEVEL=info # Logging level (debug, info, warn, error)

# ------------------------------------------------------------------------------
# 3. BACKEND API CONFIGURATION
BACKEND_EXTERNAL_PORT=8201 # The port exposed on your host machine for the Backend API

# Security: Secret key used to sign JWT tokens.
# /!\ IMPORTANT: Change this to a random long string in production /!\
JWT_SECRET=your-secure-jwt-secret-key-change-this-in-production


COOKIE_DOMAIN=localhost:8200 # Common domain for cookies (e.g., yourdomain.com if you have frontend.yourdomain.com and api.yourdomain.com)
ALLOW_UNSECURE=true # Set to 'true' if running without HTTPS (local dev). Set to 'false' in production.

# ------------------------------------------------------------------------------
# 4. SMTP CONFIGURATION (Email Service)
# Optional: Only required for password reset features.
# Leave empty if you do not wish to use email functionality.
SMTP_HOST=
SMTP_MAIL=
SMTP_PASSWORD=

# ------------------------------------------------------------------------------
# 5. FRONTEND CONFIGURATION (Nuxt)
FRONTEND_EXTERNAL_PORT=8200 # The port exposed on your host machine for the web interface