name: Build & deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Deploy to VPS
        run: |
          echo "$DEPLOY_KEY" > deploy_key
          mkdir -p ~/.ssh
          mv deploy_key ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -t rsa ${{secrets.ssh_host}} >> ~/.ssh/known_hosts
          scp -i ~/.ssh/id_rsa -r frontend/dist/* ${{secrets.ssh_host}}:~/sites/alexandrie/frontend
          scp -i ~/.ssh/id_rsa -r dashboard/dist/* ${{secrets.ssh_host}}:~/sites/alexandrie/dashboard
          scp -i ~/.ssh/id_rsa -r backend/dist/* ${{secrets.ssh_host}}:~/sites/alexandrie/backend
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
